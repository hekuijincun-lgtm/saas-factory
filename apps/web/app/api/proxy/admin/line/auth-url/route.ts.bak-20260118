import { NextRequest } from 'next/server';
import { forwardJson } from '../../../lib/proxy';

export async function GET(req: NextRequest) {
  return forwardJson(req, '/admin/integrations/line/auth-url');
}
